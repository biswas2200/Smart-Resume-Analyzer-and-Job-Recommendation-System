# Frontend (planned)

React or Angular UI (team's choice, per the project plan §6, in the
separate `docs/` project — see the repo root README) for resume upload and
the results dashboard (profile, recommended roles, gap breakdown).

Implementation starts once the [`../backend/`](../backend/README.md) API is
in place.
